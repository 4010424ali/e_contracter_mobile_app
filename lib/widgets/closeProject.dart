import 'package:flutter/material.dart';

class CloseProject extends StatefulWidget {
  @override
  _CloseProjectState createState() => _CloseProjectState();
}

class _CloseProjectState extends State<CloseProject> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Close Prject"),
    );
  }
}
